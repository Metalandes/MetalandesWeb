import type { SchemaTypeDefinition } from "sanity";
import { producto } from "./producto";

/** Tipos de contenido editables desde el Studio. */
export const schemaTypes: SchemaTypeDefinition[] = [producto];
